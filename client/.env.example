# Base origin of the deployed server — no trailing slash, no /api suffix.
# api.js appends /api for REST calls; useSocket.js uses it as-is for the
# Socket.io connection.
VITE_API_URL=http://localhost:3001
